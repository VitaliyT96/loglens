{
  "name": "asklog",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/sdk",
    "packages/parsers"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "bun test --recursive",
    "build": "tsc --build",
    "publish:packages": "npm publish --workspaces --access public"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.8.0"
  },
  "version": "0.1.0"
}
